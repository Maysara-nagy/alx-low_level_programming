#include "main.h"

/**
 * main - entry point
 * @argc: fk the count
 * @argv: fk the array of strings
 * Return: always 0
 */

int main(int argc, char *argv[])
{
	printf("%s", argv[1]);
	return (0);
}
