#include <stdio.h>

/**
 * main - entry point
 * @argc: fk the count
 * @argv: fk the array of strings
 * Return: always 0
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
