#include "main.h"

/**
 * swap_int - function to swap 2 integers
 * @a: the first parameter or first integer
 * @b: the second integer
 */

void swap_int(int *a, int *b)
{
	int tmp;
	tmp = a;
	a = b;
	b = tmp;

}
